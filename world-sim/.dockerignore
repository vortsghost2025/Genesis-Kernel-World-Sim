# Keep the docker build context minimal: the image only needs web/ (app +
# packaged snapshot) and viewer/ (the viewer page). Everything else in
# world-sim (backend, scripts, docs, tests, data) stays out of the image.
*
!web
!viewer
